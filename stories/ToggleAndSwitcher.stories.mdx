import { Fragment } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Toggle from '../labsystem/src/Toggle';

<Meta title="Components | Toggle & Switcher" component={Toggle} />

# Toggle & Switcher

The `Toggle` and `Switcher` components (...).

Both receive two inputs as props, where `type` indicates which symbol to show, while `color` defines its color.

## Sample Toggle
Toggle can be used as shown in the examples below:


<Preview>
  <Story name="Sample Toggle">
    <Toggle toggleId="sample-toggle" />
  </Story>
</Preview>


## Sample Switcher
Switcher can be used as shown in the examples below:


[insert Sample Switcher here]


## Toggle Colors

<Preview>
  <Story name="Toggle Colors">
    <Fragment>
      <Toggle />
      <Toggle />
      <Toggle />
    </Fragment>
  </Story>
</Preview>

## Switcher Colors
All the available Switcher colors are shown below:

<Preview>
  <Story name="Switcher Colors">
    <Fragment>
      <span>insert Switcher components here</span>
    </Fragment>
  </Story>
</Preview>


## Prop API docs

Here's the list of props based on the component's prop types definitions.
### Toggle
<Props of={Toggle} />

### Switcher

