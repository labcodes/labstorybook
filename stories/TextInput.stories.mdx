import { Fragment } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import TextInput from '../labsystem/src/Input/TextInput';
import EmailInput from '../labsystem/src/Input/EmailInput';
import PasswordInput from '../labsystem/src/Input/PasswordInput';
import TextInputPlayground from '../playgrounds/TextInputPlayground';

<Meta title="Components/Text Input" component={TextInput} />

<div class="docs-title">
  <div>
    <h1 class="docs-title__text">Input</h1>
  </div>
</div>

<div class="docs-content">

<p>This component is a form field that allows user to type text information. We can have 3 types of text inputs.</p>

<h2>Types</h2>
<div class="columns">
  <div class="column">
    <img src="docs/text-input/types-01.png" alt="Text and email types of inputs" />
    <p>
      This component is a form field that allows user to type text information.
      We can have 3 types of text inputs.
    </p>
  </div>
  <div class="column">
    <img src="docs/text-input/types-02.png" alt="Password input" />
    <p>
      Password input type should be styling as the Default Input with trailing icon.
      The icon must work as a toggle, showing (or hiding) the visibility of the password as the user clicks on it.
    </p>
  </div>
</div>

<h2>Anatomy</h2>
<p><strong>Field elements</strong></p>
<img src="docs/anatomy.png" alt="Input anatomy" />

<p><strong>Prefix and Suffix elements</strong></p>
<img src="docs/anatomy-02.png" alt="Prefix and suffix input anatomy" />

## Interactive demo
  This demo lets you preview the component, its variations, and configuration options.
  <TextInputPlayground />

<h2>Examples</h2>
<h3>Default Input</h3>
<p>It’s a standard text input. Can be used for enter a id, phone number, street address, email address, etc.</p>

<Canvas>
  <Story name="Default Input">
    <Fragment>
      <div className="stories-half_width-wrapper">
        <p><strong>Disabled</strong></p>
        <TextInput type="text" id="disabled" label="Disabled" disabled />
        <p><strong>Default</strong></p>
        <TextInput id="id" label="Label" />
        <p><strong>Trailing icon</strong></p>
        <TextInput className="lab-input__field--icon" id="id2" label="Label" icon="eye-opened" iconColor="mineral-70" />
        <p><strong>Error</strong></p>
        <EmailInput id="error" label="Name" defaultValue="Teste" customErrorMsg="Error message" />
      </div>
    </Fragment>
  </Story>
</Canvas>

<h3>Required Input</h3>
<p>It’s a default input that is required to be filled. It has a helper message to inform if a required input text is missing.</p>

<Canvas>
  <Story name="Required Input">
    <Fragment>
      <div className="stories-half_width-wrapper">
        <p><strong>Required Default</strong></p>
        <TextInput type="text" id="required" label="Label" helpMessage="Help message" required />
        <p><strong>Required Error</strong></p>
        <TextInput type="email" id="required2" label="Required" isValid={false} helpMessage="Help message" customErrorMsg="teste" required />
      </div>
    </Fragment>
  </Story>
</Canvas>

<h3>Prefix</h3>
<p>It’s a text field input + fixed prefix.</p>

<Canvas>
  <Story name="Prefix Input">
    <Fragment>
      <div className="stories-half_width-wrapper">
        <p><strong>Prefix Disabled</strong></p>
        <TextInput id="prefix-disabled" label="Label" prefix="CAD" disabled />
        <p><strong>Prefix Default</strong></p>
        <TextInput id="help" label="Label" prefix="R$"/>
        <p><strong>Prefix Error</strong></p>
        <TextInput
          type="email"
          id="prefix-error"
          defaultValue="Error"
          label="Label"
          prefix="R$"
          isValid={false}
          customErrorMsg="Error message"
        />
      </div>
    </Fragment>
  </Story>
</Canvas>

<h3>Suffix</h3>
<p>It’s a text field input + fixed suffix.</p>

<Canvas>
  <Story name="Suffix Input">
    <Fragment>
      <div className="stories-half_width-wrapper">
        <p><strong>Suffix Disabled</strong></p>
        <TextInput id="suffix-disabled" label="Label" suffix=".com" disabled />
        <p><strong>Suffix Default</strong></p>
        <TextInput id="suffix-address" label="Label" suffix=".com" />
        <p><strong>Suffix Error</strong></p>
        <TextInput
          type="email"
          id="suffix-error"
          label="Label"
          defaultValue="Error"
          suffix=".com"
          isValid={false}
          customErrorMsg="Error message"
        />
      </div>
    </Fragment>
  </Story>
</Canvas>

<h2>Behavior</h2>
<div class="columns">
  <div class="column">
    <h3>Default, prefix and suffix input</h3>
    <ol>
      <li>Click on the component to focus </li>
      <li>The placeholder will move up and behave like a label</li>
      <li>Type what the label suggests you to</li>
      <li>If you don't fill the input correctly, the input will show a error help message, changing the component for error state</li>
      <li>Click outside the component to unfocus</li>
    </ol>
  </div>
  <div class="column">
    <h3>Required Input</h3>
    <ol>
      <li>Click on the component to focus</li>
      <li>The placeholder will move up and behave like a label</li>
      <li>Type what the label suggests you to</li>
      <li>If you don't fill the input correctly,the input will show a error help message, changing the component for error state</li>
      <li>Click outside the component to unfocus</li>
      <li>If you don't fill the input at all, the input will show a required help message after you unfocus it,
      changing the component for error state.</li>
    </ol>
  </div>
</div>

<h3>Text, Email and Password Input Components</h3>

<Canvas>
  <Story name="Email Input Component">
  <Fragment>
    <div className="stories-half_width-wrapper">
      <TextInput id="text-input-component" label="Text Input Component" defaultValue="text example" /><br />
      <EmailInput id="email-input-component" label="Email Input Component" defaultValue="email@example.com" /><br />
      <PasswordInput id="password-input-component" label="Password Input Component" defaultValue="passwordExample"/>
    </div>
  </Fragment>
  </Story>
</Canvas>

<h2>Theming</h2>
<p>For text inputs, you can also use purple theme. Error state remain with the same style in any theme. The theme must be selected by changing the value of the <code>$input-theme</code> variable in the <code>scss/variables/_settings.scss</code>.</p>

<Canvas>
  <Story name="Styles">
  <Fragment>
    <div className="stories-half_width-wrapper lab-input--stories-force-purple">
      <p><strong>Default</strong></p>
        <TextInput defaultValue="Purple" id="purple1" label="Label" />
      <p><strong>Required Default</strong></p>
        <TextInput id="purple2" label="Label" defaultValue="Purple"
        helpMessage="Help message" required />
      <p><strong>Prefix Default</strong></p>
        <TextInput defaultValue="Purple"
        id="purple3" label="Label" prefix="R$" />
      <p><strong>Suffix Default</strong></p>
        <TextInput defaultValue="Purple"
        id="suffix-address-purple" label="Label" suffix=".com" />
    </div>
  </Fragment>
  </Story>
</Canvas>

<h2>Prop API docs</h2>
<p>Here's the list of props based on the component's prop types definitions.</p>

<h3>TextInput</h3>
<ArgsTable of={TextInput} />

<h3>EmailInput</h3>
<ArgsTable of={EmailInput} />

<h3>PasswordInput</h3>
<ArgsTable of={PasswordInput} />

</div>
