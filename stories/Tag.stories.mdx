import { Fragment } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Tag from '../labsystem/src/Tag';

<Meta title="Components | Tag" component={Tag} />

# Tag

The `Tag` component might receive up to five inputs as props, where the `color` and `name` parameters are the only required ones.
- `name`: should be an unique identifier and works as the `id` of the `input` tag within the component; it's also an important accessibility tool
- `color`: defines the Tag color
- `disabled`: sets the Tag component as disabled; includes functionality and styles
- `defaultValue`: you can set a default value for the Tag to start with; it normally starts as `false`
- `value`: you might need to set the component value from outside the component; if so, the internal logic to handle the component value won't be used
- `handleTag`: you might need to perform other actions when the component value changes; use this param to pass a function that's gonna be called on value changes

## Sample Tag
Tag can be used as shown in the example below:

<Preview>
  <Story name="Soft Tag">
    <Fragment>
      <Tag>Label</Tag>
      <Tag style="outline">Label</Tag>
      <Tag icon="calendar">Label</Tag>
      <Tag style="outline" icon="calendar">Label</Tag>
      <Tag dropdown>Label</Tag>
      <Tag dropdown style="outline">Label</Tag>
      <Tag removable>Label</Tag>
      <Tag removable style="outline">Label</Tag>
      <Tag thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="token image">Label</Tag>
      <Tag style="outline" thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="token image">Label</Tag>
    </Fragment>
  </Story>
</Preview>

<Preview>
  <Story name="Intense Tag">
    <Fragment>
      <Tag colorStyle="intense">Label</Tag>
      <Tag colorStyle="intense" style="outline">Label</Tag>
      <Tag colorStyle="intense" icon="calendar">Label</Tag>
      <Tag colorStyle="intense" style="outline" icon="calendar">Label</Tag>
      <Tag colorStyle="intense" dropdown>Label</Tag>
      <Tag colorStyle="intense" dropdown style="outline">Label</Tag>
      <Tag colorStyle="intense" removable>Label</Tag>
      <Tag colorStyle="intense" removable style="outline">Label</Tag>
      <Tag colorStyle="intense" thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="token image">Label</Tag>
      <Tag colorStyle="intense" style="outline" thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="token image">Label</Tag>
    </Fragment>
  </Story>
</Preview>

## Tag themes
Tag can be used in the following themes:

<Preview>
  <Story name="Sample Tag Themes">
    <Fragment>
      <Tag color="purple" style="outline">Daniela Ágatha</Tag>
      <Tag thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="token image">With thumb</Tag>
      <Tag thumbSrc="docs/tokens-spacing/10-spacing.png" thumbAlt="">With thumb</Tag>
    </Fragment>
  </Story>
</Preview>

## Tag Disabled
Togge can be disabled and, at the same time receive, a `defaultValue`:

<Preview>
  <Story name="Sample Tag Disabled">
    <Fragment>
      <Tag disabled>Disabled</Tag>
      <Tag disabled style="outline">Disabled</Tag>
    </Fragment>
  </Story>
</Preview>

## Prop API docs

Here's the list of props based on the component's prop types definitions.
### Tag
<Props of={Tag} />


### TODO

Dúvidas
- Ver pq o operador ternário é melhor que o if para devs. A gente acha o if mais legível
- Dentro das {} tem ou não espaço?
- Queremos um guideline de react

Todos
- Ajeitar ícone 
- Mostrar erro quando passa na thumb src sem alt
- Estados aberto/fechado pra dropdowns
- Estados selecionado/idle pra chips

tag
-> simples sem interação. com ícone ou thumb de elemento
-> dropdowns. com ícone de elemento
-> removable. com ícone ou thumb de elemnto