import { Fragment } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Inputs from '../labsystem/src/Inputs';

<Meta title="Components | Text Inputs" component={Inputs} />


# General usage

This component is a form field that allows user to type text information.
We can have 4 types of text inputs:

## Anatomy

### Field elements
![input anatomy](docs/anatomy.png)

### Prefix and Suffix elements
![prefix and suffix input anatomy](docs/anatomy-02.png)
<br />

## Default Input

It’s a standard text input. Can be used for enter a name, phone number, street address, email address, etc.

<Preview>
  <Story name="Default Input">
    <Fragment>
      <h3>Elemento vazio</h3>
      <input type="text" />
      <h3>Disabled</h3>
      <Inputs type="text" id="disabled" label="Disabled" disabled />
      <br />
      <h3>Default</h3>
      <Inputs id="name" label="Label" />
      <br />
      <h3>Trailing icon</h3>
      <Inputs id="name2" label="Label" icon="eye-opened" iconColor="mineral70" />
      <br />
      <h3>Error</h3>
      <Inputs type="email" id="error" label="Name" value="Error" />
    </Fragment>
  </Story>
</Preview>

## Required Input

It’s a default input that is required to be filled. It has a helper message to inform if a required input text is missing.

<Preview>
  <Story name="Required Input">
    <Fragment>
      <h3>Required Default</h3>
      <Inputs type="text" id="required" label= "Label" helpMessage="Help message" required />
      
      <h3>Required Error</h3>
      <Inputs type="email" id="required2" value="Error" label= "Required" required />
    </Fragment>
  </Story>
</Preview>

## Prefix

It’s a text field input + fixed prefix.

<Preview>
  <Story name="Prefix Input">
    <Fragment>
      <h3>Prefix Disabled</h3>
      <Inputs className="lab-input__field--prefix" id="prefix" label="Label" prefix="R$" disabled />
      <br />
      
      <h3>Prefix Default</h3>
      <Inputs className="lab-input__field--prefix" id="help" label="Label" prefix="R$" />
      <br />
      
      <h3>Prefix Error</h3>
      <Inputs
        className="lab-input__field--prefix"
        type="email"
        id="prefix-error"
        value="Error"
        label="Label"
        prefix="R$" />
    </Fragment>
  </Story>
</Preview>

## Suffix

It’s a text field input + fixed suffix.

<Preview>
  <Story name="Suffix Input">
    <Fragment>
      <h3>Suffix Disabled</h3>
      <Inputs className="lab-input__field--suffix" id="suffix" suffix=".com" disabled />
      <br/>
      
      <h3>Suffix Default</h3>
      <Inputs className="lab-input__field--suffix" id="address" suffix=".com" />
      <br />
      
      <h3>Suffix Error</h3>
      <Inputs className="lab-input__field--suffix" type="email" id="suffix-error" value="Error" suffix=".com" />
    </Fragment>
  </Story>
</Preview>

## Behavior
### Default input/ Prefix and suffix input

1. Click on the component to focus
2. The placeholder will move up and behave like a label
3. Type what the label suggests you to
4. If you don't fill the input correctly, the input will show a error help message,
changing the component for error state
5. Click outside the component to unfocus

### Required Input

1. Click on the component to focus
2. The placeholder will move up and behave like a label
3. Type what the label suggests you to
4. If you don't fill the input correctly,
the input will show a error help message, changing the component for error state
5. Click outside the component to unfocus
6. If you don't fill the input at all, the input will show a required help
message after you unfocus it, changing the component for error state.

## Styles

For text inputs, you can use this other color scheme
<Preview>
  <Story name="Styles">
  <Fragment>
    <div className="input-colors base">
      base border
    </div>
    <div className="input-colors hover">
      <span>hover border</span>
    </div>
    <div className="input-colors hover-border">
      base border : hover
    </div>
    <div className="input-colors focus">
      focus border
    </div>
    <div className="input-colors focus-border">
      base border : focus    
    </div>
    <div className="input-colors prefix">
      prefix / suffix area    
    </div>
  </Fragment>
  </Story>
</Preview>
