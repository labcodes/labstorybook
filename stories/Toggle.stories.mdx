import { Fragment } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import Toggle from '../labsystem/src/Toggle';

<Meta title="Components/Toggle" component={Toggle} />

<div class="docs-title">
  <div>
    <h1 class="docs-title__text">Toggle</h1>
  </div>
</div>

<div class="docs-content">

The `Toggle` component might receive up to five inputs as props, where the `color` and `name` parameters are the only required ones.
- `name`: should be an unique identifier and works as the `id` of the `input` tag within the component; it's also an important accessibility tool
- `color`: defines the Toggle color
- `disabled`: sets the Toggle component as disabled; includes functionality and styles
- `defaultValue`: you can set a default value for the toggle to start with; it normally starts as `false`
- `value`: you might need to set the component value from outside the component; if so, the internal logic to handle the component value won't be used
- `handleToggle`: you might need to perform other actions when the component value changes; use this param to pass a function that's gonna be called on value changes

## Sample Toggle
Toggle can be used as shown in the example below:


<Canvas>
  <Story name="Sample Toggle">
    <Toggle name="sample-toggle-1" />
  </Story>
</Canvas>

## Toggle themes
Toggle can be used in the following themes:

<Canvas>
  <Story name="Sample Toggle Themes">
    <Fragment>
      <Toggle name="sample-toggle-teal" color="teal"/><br /><br />
      <Toggle name="sample-toggle-purple" color="purple" defaultValue={true} /><br /><br />
    </Fragment>
  </Story>
</Canvas>

## Toggle Disabled
Togge can be disabled and, at the same time receive, a `defaultValue`:

<Canvas>
  <Story name="Sample Toggle Disabled">
    <Fragment>
      <Toggle name="sample-toggle-disabled-off" disabled /><br /><br />
      <Toggle name="sample-toggle-disabled-on" disabled defaultValue={true} />
    </Fragment>
  </Story>
</Canvas>

## Prop API docs

Here's the list of props based on the component's prop types definitions.
### Toggle
<ArgsTable of={Toggle} />

</div>
