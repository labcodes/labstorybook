import { Fragment } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Input from '../labsystem/src/Input';
import InputBehaviorTest from '../labsystem/src/InputBehaviorTest';

<Meta title="Components | Text Inputs" component={Input} />

<div class="docs-title">
  <div>
    <h1 class="docs-title__text">Inputs</h1>
  </div>
</div>

<div class="docs-content">

### General usage

This component is a form field that allows user to type text information.
We can have 4 types of text inputs:

### Anatomy

###### Field elements
![input anatomy](docs/anatomy.png)

###### Prefix and Suffix elements
![prefix and suffix input anatomy](docs/anatomy-02.png)

### Default Input

It’s a standard text input. Can be used for enter a id, phone number, street address, email address, etc.

<Preview>
  <Story name="Default Input">
    <Fragment>
      <h6>Disabled</h6>
      <Input type="text" id="disabled" label="Disabled" disabled />
      <h6>Default</h6>
      <Input id="id" label="Label" />
      <h6>Trailing icon</h6>
      <Input className="lab-input__field--icon" id="id2" label="Label" icon="eye-opened" iconColor="mineral70" />
      <h6>Error</h6>
      <Input type="email" id="error" label="Name" defaultValue="Teste" customErrorMsg="Error message" />
    </Fragment>
  </Story>
</Preview>

### Required Input

It’s a default input that is required to be filled. It has a helper message to inform if a required input text is missing.

<Preview>
  <Story name="Required Input">
    <Fragment>
      <h6>Required Default</h6>
      <Input type="text" id="required" label= "Label" helpMessage="Help message" required />
      <h6>Required Error</h6>
      <Input type="email" id="required2" label= "Required" isValid={false} helpMessage="Help message" customErrorMsg="teste" required />
    </Fragment>
  </Story>
</Preview>

### Prefix

It’s a text field input + fixed prefix.

<Preview>
  <Story name="Prefix Input">
    <Fragment>
      <h6>Prefix Disabled</h6>
      <Input id="prefix" label="Label" prefix="Raaaa" disabled />
      <br />
      <h6>Prefix Default</h6>
      <Input id="help" label="Label" prefix="aaaaaa$" suffix=".com"/>
      <br />
      <h6>Prefix Error</h6>
      <Input
        type="email"
        id="prefix-error"
        defaultValue="Error"
        label="Label"
        prefix="R$"
        isValid={false}
        customErrorMsg="Error message"
      />
    </Fragment>
  </Story>
</Preview>

### Suffix

It’s a text field input + fixed suffix.

<Preview>
  <Story name="Suffix Input">
    <Fragment>
      <h6>Suffix Disabled</h6>
      <Input id="suffix" label="Label" suffix=".com" disabled />
      <br/>
      <h6>Suffix Default</h6>
      <Input id="address" label="Label" suffix=".com" />
      <br />
      <h6>Suffix Error</h6>
      <Input
        type="email"
        id="suffix-error"
        label="Label"
        defaultValue="Error"
        suffix=".com"
        isValid={false}
        customErrorMsg="Error message"
      />
    </Fragment>
  </Story>
</Preview>

### Behavior
##### Default input/ Prefix and suffix input

1. Click on the component to focus
2. The placeholder will move up and behave like a label
3. Type what the label suggests you to
4. If you don't fill the input correctly, the input will show a error help message,
changing the component for error state
5. Click outside the component to unfocus

##### Required Input

1. Click on the component to focus
2. The placeholder will move up and behave like a label
3. Type what the label suggests you to
4. If you don't fill the input correctly,
the input will show a error help message, changing the component for error state
5. Click outside the component to unfocus
6. If you don't fill the input at all, the input will show a required help
message after you unfocus it, changing the component for error state.

### Input Playground

<Preview>
  <Story name="Input Playground">
  <Fragment>
    <InputBehaviorTest />
  </Fragment>
  </Story>
</Preview>

### Theming

For text inputs, you can also use purple theme. Error state remain with the same style in any theme.
<Preview>
  <Story name="Styles">
  <Fragment>
    <h6>Default</h6>
      <Input className="lab-input--purple" defaultValue="Purple" id="purple1" label="Label" />
    <h6>Required Default</h6>
      <Input className="lab-input--purple" id="purple2" label= "Label" defaultValue="Purple"
      helpMessage="Help message" required />
    <h6>Prefix Default</h6>
      <Input className="lab-input--purple" defaultValue="Purple"
      id="purple3" label="Label" prefix="R$" />
    <br />
    <h6>Suffix Default</h6>
    <Input className="lab-input--purple" defaultValue="Purple"
    id="address" label="Label" suffix=".com" />
    <br />
  </Fragment>
  </Story>
</Preview>

</div>
