import { Fragment } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import ExampleButton from '../labsystem/src/ExampleButton';

<Meta title="Components | Buttons" component={ExampleButton} />

# Example Button

This MDX example shows us how to use the `@storybook/addon-docs` to have a "design system" like use for storybooks.

We'll show you how to document things and all. Here we go!

## Default ExampleButton - Primary

This is the place for better explaining why your ExampleButton defaults to 'primary'. Then, there's the preview:

<Preview>
  <Story name="Default ExampleButton - Primary">
    <ExampleButton>Click me!</ExampleButton>
  </Story>
</Preview>

## ExampleButton Variants

This is the place for better explaining the possible variants of your button, for example.

<Preview>
  <Story name="ExampleButton Variants">
    <Fragment>
      <ExampleButton type="primary">Primary</ExampleButton>
      <ExampleButton type="secondary">Secondary</ExampleButton>
      <ExampleButton type="info">Info</ExampleButton>
      <ExampleButton type="danger">Danger</ExampleButton>
      <ExampleButton type="success">Success</ExampleButton>
    </Fragment>
  </Story>
</Preview>

## Prop API docs

Here's the list of props based on the component's prop types definitions.

<Props of={ExampleButton} />
